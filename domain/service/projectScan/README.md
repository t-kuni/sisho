# scan()

* プロジェクトスキャンを実装する
* プロジェクトルートの `.sishoignore` を読み込み、ここに記載されているパスはスキャン対象から除外する
  * github.com/denormal/go-gitignore を使用する
* 各階層での処理本体は引数のクロージャにて実装する
* 呼び出し元に進捗を通知するためのクロージャを別途作成する
  * フォルダに入る時、フォルダ・ファイルをスキップするタイミング等でクロージャを呼び出す